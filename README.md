# casemask
🔒 Local-first 医疗病历脱敏工作台 —— PDF/DOCX/MD 一键归一为 Markdown，圈选建规则，打包导出，病历数据永不出机。 (Local-first de-identification workbench for clinical documents: convert to Markdown, mask by selection, export as bundle — data never leaves your machine.)
